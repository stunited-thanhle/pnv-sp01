export enum ModalTypeEnum {
  CONFIRM = 'confirm',
  SUCCESS = 'success',
}

export enum StatusType {
  ON = 'ON',
  OFF = 'OFF',
}

export enum ROLE {
  SUPER_ADMIN = 'super-admin',
  ADMIN = 'admin',
  NORMAL = 'normal',
}
